export default function MarkedDate({children}:{children:React.ReactNode}){
    return(
        <p className="bg-violet-400 bg-opacity-10 rounded px-1 py-0.5 w-full text-center text-violet-400">{children}</p>
    )
}